const b = [
    document.querySelector(".navbar"),
    document.querySelector(".fullscreen"),
  ],
  c = document.querySelectorAll("h3"),
  d = document.querySelectorAll(".tool"),
  e = document.querySelectorAll(".formation"),
  f = document.querySelectorAll(".project"),
  a = i(2);
function h(a) {
  if ("number" != typeof a) throw new Error("Parameter must be a number");
  if (a > 15) throw new Error("Number can't be higher than 15");
  return 100 * a;
}
function i(a) {
  return { reset: !0, delay: h(a) };
}

ScrollReveal().reveal(b, a),
  ScrollReveal().reveal(e, a),
  ScrollReveal().reveal(c, a),
  ScrollReveal().reveal(d, a),
  ScrollReveal().reveal(f, a);

new TypeIt("#hero", {
  speed: 50,
  startDelay: 900,
})
  .type("Desarrolladr Web Ful Stack", { delay: 100 })
  .move(-6, { delay: 150 })
  .type("l", { delay: 200 })
  .move(-10, { delay: 150 })
  .type("o", { delay: 200 })
  .move(null, { to: "END" })
  .go();
